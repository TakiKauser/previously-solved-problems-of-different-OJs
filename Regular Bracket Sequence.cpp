#include<bits/stdc++.h>
using namespace std;
int main(){
    long long int a, b, c, d;
    cin >> a >> b >> c >> d;
    if(a!=d || c>0 && a==0) cout << 0 << endl;
    else cout << 1 << endl;
return 0;
}
